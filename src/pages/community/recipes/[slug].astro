---
import Layout from '../../../layouts/Layout.astro';

export function getStaticPaths() {
  // Placeholder — will be populated from Sanity CMS in Phase 3
  return [
    { params: { slug: 'example' } },
  ];
}

const { slug } = Astro.params;
---
<Layout title={`${slug} | Jewmanity`}>
  <div class="min-h-screen flex items-center justify-center flex-col gap-4">
    <h1 class="text-3xl font-heading font-medium text-text-heading">{slug}</h1>
    <p class="text-text-muted">Dynamic page — Phase 1</p>
  </div>
</Layout>
